import {GameMap} from "./game-map"
import {BadgeAlert} from "./badge-alert"
import {Section} from "./section"
import Typography from "@mui/material/Typography"

export const Tutorial = () => {
    return (
        <Section>
            <Typography p={1}>
                Paper tactics is a pen-and-paper game.
            </Typography>
        </Section>
    )
}
